# Isolated voice-clone environment.
# Keep this separate from requirements.txt because WhisperX requires transformers 4.x.
suno-bark>=0.0.1a0
omnivoice>=0.1.5
transformers>=5.3.0
torch>=2.8.0
torchaudio>=2.8.0
scipy>=1.15.0
numpy>=2.1.0
